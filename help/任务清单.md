# LPT v2 任务清单

## 文件定位

本文件是当前 v2-only 主线的任务清单。旧清单已从 `.tmp_lpt_v1_ds_archive/help/任务清单.md` 迁移到这里，并按 LPT v2 架构、checkpoint、状态池、训练入口和评测工具重新定义。

其他 Markdown 文件可以作为设计方案、实验报告、命令说明或背景材料，但不能自动改变本文件中的任务状态。任何任务完成度、优先级、成功标准或后续计划变更，都必须直接回写本文件。

状态标记约定：

- `[x]` 已完成，并已有代码或测试依据。
- `[ ]` 未完成或整体未完成。
- “部分完成”写在任务说明内，不用勾选代替。

## 迁移原则

- v2 主线只接受 `LPTV2`、`LayerStateV2`、当前 `ModelConfig` schema、`checkpoint_format="lpt_v2_checkpoint"` 与 `architecture_version="lpt_v2"`。
- v1 checkpoint、旧 `ModelConfig`、旧训练 recipe、旧 LoRA、旧推理 session 和旧 checkpoint loader 不做兼容迁移。
- 旧任务若仍有价值，必须升级为 v2 任务：使用 `lpt_config/`、`lpt_model/`、`lpt_runtime/`、`lpt_data/`、`lpt_training/`、`lpt_workflows/`、`lpt_inference/`、`lpt_lora/`、`lpt_eval/` 和 `tools/` 下的正式模块。
- `.tmp_lpt_v1_ds_archive/` 只允许只读查阅，禁止作为运行时 import 依赖。
- 后续脚本与文档默认同时面向 Windows 10 / Windows Server 2019+ 和 Linux Ubuntu 22.04+ / CentOS 8+；路径处理优先使用 `pathlib.Path`。

## 当前结论

截至 2026-05-11，v1 原任务清单中的基础工程闭环已经完成 v2 化迁移：配置快照、严格 checkpoint、三阶段训练 workflow、训练循环、结构化数据、sequence packing、LongRoPE2 工作流参数、推理 session、LoRA adapter、评测报告和执行层基础都已进入 v2 主线；LongRoPE2 factor sweep 与长上下文 suite 已补齐为 v2-native 工具。

当前不再把“恢复 v1 能力”作为目标，而是继续推进以下 v2 任务：

1. 基于真实训练 checkpoint 跑长上下文 suite、LongRoPE2 factor sweep 和资源成本报告。
2. 补齐正式数据训练入口的长跑验证。
3. 建立 sequence packing benchmark 的固定回归口径。
4. 完成分布式执行 v2：真实多卡推理 smoke、Tensor Parallelism、FSDP2、分布式状态治理。
5. 建立 v2 实验治理目录和 tokenizer / manifest / checkpoint / git commit 绑定。
6. 补服务化推理入口，并复用现有 `InferenceSession` 与执行计划。

2026-08-02 范围补充：新增训练提交正确性、纯文本拒绝账本、版本化数据加速缓存和 KV0-KV5 分阶段任务。后续技术吸收采用“成熟且兼容的技术先按依赖组成候选包、统一测速、发生回归后再定向拆分”的原则；正确性与可恢复性约束直接实施，不作为可选消融项。各项完成状态仍以对应任务条目为准。

## 当前工程基线

- v2 模型主干：`LPTV2`、`LPTBlockV2`、Local SDPA Attention、Paged KV、Shared RetNetAssist Q Adapter、SwiGLU-MoE、xLSTMAssist。
- v2 状态：`LayerStateV2`、RetNetAssist request-bound state pool、xLSTM request-bound state pool、Paged KV page pool。
- v2 配置：`ModelConfig` 的 `model_config_schema_version=3`、多规格 preset、LongRoPE2 因子工具、MoE-aware 参数统计。
- v2 训练：`text_pretrain / chat_sft / chat_lora` 三阶段 workflow、正式训练循环、resume、optimizer/scheduler 状态、manifest 数据进度账本、JSONL/TensorBoard 指标、周期性验证、LoRA adapter 保存。
- v2 数据：结构化 `text / chat` schema、manifest、流式加载、转换工具、sequence packing 和 token-based batching。
- v2 推理：`InferenceSession`、token-id 推理工具、checkpoint 摘要展示、推理执行计划。
- v2 评测：profile 基线、长上下文机制准入、资源报告、xLSTMAssist 专项评测。
- v2 治理：checkpoint schema v2 严格 loader、tokenizer metadata 随 trainer state 保存、v2 命令文档。

## 不变约束

- 禁止混用 v1 checkpoint、v1 tokenizer 元数据、v1 训练产物与 v2 模型。
- 后续训练、推理、评测、benchmark 和报告必须显式绑定 tokenizer 元数据。
- checkpoint loader 只接受当前 v2 schema；不为旧 `.pth` 或旧 schema 放宽校验。
- 模型结构必须通过 `ModelConfig` 或 checkpoint 内的完整 `model_config` 快照恢复。
- LongRoPE2 候选因子 sweep 只能临时覆盖评测进程内配置，不得写回 checkpoint 或训练产物。
- `CUDA_VISIBLE_DEVICES` 是单机多卡资源池边界；程序内部 device map 只使用 PyTorch 暴露后的逻辑设备号，例如 `cuda:0 / cuda:1`。

## P0：旧 P0 工程闭环的 v2 迁移

- [x] 1. 迁移 `InferenceSession / CacheManager` 语义到 v2 request-bound 状态体系
  - v2 范围：`InferenceSession` 负责 prefill/decode/rebuild/reset/export，Paged KV 与 RetNetAssist/xLSTM 状态由模型和状态池维护。
  - 成功标准：调用方不直接操作裸 `LayerStateV2`；session reset 能释放 request-bound Assist state；LongRoPE2 策略切换可显式重建缓存。
  - 当前结果：`lpt_inference/session.py` 已落地 v2 `InferenceSession`；`lpt_model/state_pool_v2.py` 已提供 RetNetAssist/xLSTM request state pool；Paged KV 与 Assist state 物理隔离。

- [x] 2. 迁移 checkpoint schema 到 v2
  - v2 范围：保存 `checkpoint_format`、`checkpoint_schema_version=2`、`architecture_version="lpt_v2"`、完整 `model_config` 快照、runtime metadata 和 LayerStateV2 schema 元数据。
  - 成功标准：loader 严格拒绝 schema、architecture 或 runtime metadata 不匹配的 checkpoint。
  - 当前结果：`lpt_model/checkpoint_v2.py` 已实现保存、校验和严格加载；测试覆盖 schema/architecture 拒绝与保存加载 round-trip。

- [ ] 3. 迁移长上下文评测为 v2 准入报告
  - v2 范围：needle、长文本 PPL、QA/retrieval 代理指标、code/math 代理指标、format following 代理指标、RetNetAssist 机制差异。
  - 成功标准：未加载训练 checkpoint 时只给机制准入，不伪造质量收益；加载真实 checkpoint 后可作为质量准入依据。
  - 当前结果：部分完成。`lpt_eval/long_context.py` 与 `tools/run_lpt_v2_long_context_eval.py` 已落地随机初始化机制准入报告，CUDA smoke 可输出 JSON / Markdown；但当前 API/CLI 只按 `preset / vocab_size` 构造随机模型，没有 `--checkpoint` 或外部模型注入入口，尚不能加载真实训练 checkpoint 做质量准入。
  - 后续任务：为 `run_lpt_v2_long_context_admission()` 和 CLI 增加严格 v2 checkpoint 加载路径，报告绑定 checkpoint 摘要、tokenizer metadata、profile/preset、attention/cache backend 和执行设备。

- [x] 4. 迁移模型结构配置为 v2 可序列化 `ModelConfig`
  - v2 范围：多规格 preset、Attention backend、Paged KV、RetNetAssist、MoE、xLSTMAssist、LongRoPE2、参数统计口径。
  - 成功标准：配置可 JSON 序列化；checkpoint 可严格恢复；不兼容 schema 被明确拒绝。
  - 当前结果：`lpt_config/model_config.py` 已落地 `model_config_schema_version=3` 与 v2 约束校验。

## P1：旧 P1 训练现代化能力的 v2 迁移

- [x] 5. 迁移完整训练 recipe 到 v2
  - v2 范围：AdamW 参数分组、weight decay 白名单/黑名单、梯度累积、梯度裁剪、固定 seed、周期性验证、resume、JSONL/TensorBoard 指标、checkpoint 与推理权重保存。
  - 成功标准：固定 seed 下短程训练可复现；resume 前后 `global_step / optimizer_step / lr` 连续；日志可追踪 `loss / lr / tokens_per_sec / grad_norm / eval_loss / eval_ppl`。
  - 当前结果：`lpt_training/train.py` 与三阶段 workflow 已落地；`tests/test_lpt_v2_workflows.py` 覆盖训练循环保存 v2 checkpoint、trainer state、manifest `data_progress.json` 和 LoRA round-trip。
  - 2026-05-23 补充：checkpoint 现在原子保存 `data_progress.json`，按当前 manifest 活跃集合续训；移除、注释或 `weight=0` 的历史数据集会保留进度但跳过，新增数据集从 0 补齐目标轮数与条数；已训练数据集的非零 `weight / sample_limit` 或文件内容变化会 fail closed，避免进度错位。

- [x] 6. 迁移 `sequence packing / token-based batching` 到 v2 数据管线
  - v2 范围：结构化 `text / chat` 样本编码、样本边界保留、`position_ids / segment_ids`、padding 与 packing。
  - 成功标准：同一 packed row 内不发生跨样本监督泄漏；模型 forward 能接收 `segment_ids`。
  - 当前结果：`lpt_data/batching.py` 已实现 v2 packing；`lpt_training/train.py` 默认启用 sequence packing。

- [x] 7. 迁移 LongRoPE2 训练策略到 v2 workflow
  - v2 范围：训练/推理 RoPE cache 长度、original/target window、long factors 文件、train/inference embedding mode、mixed original window、训练 batch 窗口混合采样。
  - 成功标准：LongRoPE2 不只是推理侧覆盖项；训练、评测和 checkpoint 配置边界清晰。
  - 当前结果：`lpt_workflows/common.py` 已统一注册 LongRoPE2 workflow 参数；`lpt_training/train.py` 已支持 batch 级窗口采样。

- [x] 7.1. 迁移 LongRoPE2 候选因子 sweep 评估器到 v2
  - v1 来源：旧清单中 `tools/evaluate_longrope2_factor_sweep.py` 与 `lpt_evaluation/longrope2_factor_sweep.py` 已在 v1 完成。
  - v2 范围：实现 `lpt_eval/longrope2_factor_sweep.py` 与 `tools/run_lpt_v2_longrope2_factor_sweep.py`，输入 current、bootstrap、uniform、factors-file 多来源候选因子，复用 v2 长上下文准入指标。
  - 成功标准：输出统一 JSON / Markdown；候选因子只作用于评测进程内 `ModelConfig`，不写回 checkpoint；报告绑定 checkpoint schema、训练阶段、step、候选因子来源与长上下文代理指标。
  - 当前结果：已新增 `lpt_eval/longrope2_factor_sweep.py` 与 `tools/run_lpt_v2_longrope2_factor_sweep.py`；同时新增 `run_lpt_v2_long_context_suite.py`，支持多 `seq_len / attention_window / needle_depth` 组合，并复用真实 checkpoint 准入口径。

- [x] 7.2. 增加 v2 原生 Thinking 模式
  - 需求边界：Thinking 必须是可控、原生的模型能力，不使用 `<think>`、`</think>` 等自然文本边界作为控制机制；旧含标签数据只允许通过一次性转换脚本修正为结构化字段，不在训练、推理或运行时协议中做兼容。
  - 阶段语义：`text_pretrain` 保持原有 text 数据格式与训练路径，默认 `thinking=off`；`chat_sft` 与 `chat_lora` 阶段读取 chat 多轮消息时，`role="assistant"` 且 `thinking` 字段非空的轮次视为 `thinking=on`，缺失或空字符串视为 `thinking=off`。
  - 数据结构：保留现有多轮 `messages` 能力，只扩展 assistant 消息字段，不改成单独顶层 assistant 对象。正式结构示例：

  ```json
  {
    "type": "chat",
    "messages": [
      {"role": "system", "content": "可选系统提示"},
      {"role": "user", "content": "用户问题"},
      {
        "role": "assistant",
        "thinking": "内部思考内容",
        "content": "最终回答"
      }
    ]
  }
  ```

  - Schema 任务：扩展 `lpt_protocol.template.validate_messages()` 与 `lpt_data.schema.normalize_dataset_record()`，允许 assistant 消息携带可选 `thinking` 字符串；禁止 `system / user / observation` 等非 assistant 消息携带 `thinking`；空白 `thinking` 归一为空并按 off 处理。
  - 数据转换任务：修正 `tools/convert_reasoning_chat_sft_datasets.py`，把 `instruction / think / response` 转为 `messages=[user, assistant]`，其中 `think` 写入 assistant 的 `thinking`，`response` 写入 assistant 的 `content`；禁止继续把 thinking 包装进 `<think>...</think>`。新增或修正旧标签数据清洗逻辑：解析已有 `<think>...</think>` 内容并落到 `thinking` 字段，剩余最终回答回写 `content`。
  - 数据落位任务：`data/z-pending-data/thinking/` 只作为待处理源数据池；转换后进入 `data/structured/chat_sft/thinking/` 或 `data/structured/chat_lora/thinking/`，再由 manifest 引用。manifest 的 `weight / sample_limit / path` 等既有语义保持不变，`data_progress.json` 继续按文件粒度追踪。
  - 训练编码任务：训练 batch 增加原生 thinking 控制信息，例如 `thinking_mode_id`、`target_channel_id` 或等价结构；answer 与 thinking 目标需要可分离监督。`thinking=off` 的样本只监督最终回答；`thinking=on` 的样本监督 thinking 通道和最终回答通道。
  - 模型任务：在 `ModelConfig` 和 `LPTV2` 中增加原生 thinking 控制能力，例如 `thinking_mode` 枚举、mode embedding/control embedding 或等价条件信号；控制信号来自结构化 batch 与推理参数，不来自 prompt 文本。
  - 推理任务：推理入口支持 `thinking_mode=off|on|auto`。`off` 必须强制不生成 thinking；`on` 生成 thinking 与 answer；`auto` 由策略决定是否启用，但必须能被 `off` 覆盖。
  - 展示任务：将“是否生成 thinking”和“是否展示 thinking”拆开，增加 `thinking_visibility=hidden|visible` 或等价参数。`thinking=on` 且 visible 时返回结构化 `thinking` 字段；hidden 时只返回 answer，但不得把 thinking 混入 answer。
  - Checkpoint 与实验治理：checkpoint runtime metadata、trainer state、metrics 和实验报告记录 thinking schema 版本、训练阶段实际 thinking 样本统计、推理 thinking 参数与可见性参数，确保后续 checkpoint 可审计。
  - 测试任务：补 schema 测试、converter 测试、训练渲染/批处理测试、推理参数测试和无 `<think>` 回归测试。重点验证多轮 messages 不被破坏、非 assistant 禁止 `thinking`、`thinking=off` 不输出思考内容、旧 `<think>` 数据转换后正式样本不再含标签。
  - 当前结果：已落地结构化 assistant `thinking` 字段校验、训练 batch 原生 `thinking_mode_ids / target_channel_ids`、`LPTV2` mode/channel control embedding、推理 `thinking_mode=off|on|auto` 与 `thinking_visibility=hidden|visible`、checkpoint state_schema thinking metadata、trainer_state thinking 统计和 converter 清洗路径。`ModelConfig` 的 `model_config_schema_version` 已升到 3，旧配置 schema checkpoint 不做隐式兼容；当前 `data/structured/chat_sft/` 下旧 `<think>` 数据已一次性转换为 `thinking` 字段。2026-05-24 补充：chat SFT 与 chat LoRA 训练 recipe 默认 `thinking_mode=auto`，仅当 assistant 消息 `thinking` 字段存在且去空白后非空时走 thinking on 分支；`thinking` 缺失或为空字符串时走 off 分支。训练 `thinking_visibility=hidden|visible` 只写入 trainer_state/checkpoint 审计元数据，不影响 batch、loss 或模型前向。统一推理入口中 `chat_sft / lora / chat_lora` 默认 `thinking_mode=on` 且 `thinking_visibility=visible`，`text_pretrain` 默认保持 `off/hidden`，CLI 显式参数仍可覆盖。

## P1.5：训练工程与显存优化专项

- [ ] 7.3. 降低训练显存峰值并建立显存口径
  - 背景：当前训练显存主要由参数、梯度、优化器状态、反向激活、完整 `logits=[batch, seq, vocab]`、attention 中间张量、RoPE cache 和 PyTorch reserved cache 共同决定；`nvidia-smi` 口径会高于 `torch.cuda.max_memory_allocated()`，因此指标和优化必须分开治理。
  - 成功标准：训练日志同时记录 allocated/reserved 当前值与峰值；优化项通过 benchmark 对比 `sequence_length / target_tokens / tokens_per_sec / allocated_peak / reserved_peak / loss`；不使用每步 `torch.cuda.empty_cache()` 作为默认降显存手段。
  - 落地清单：
    - [ ] 1. 实现 `chunked lm_head + loss`：模型训练路径返回最后 hidden states，训练循环按 token chunk 调用 tied `lm_head` 并立即计算 CE，避免完整 `[B,S,V] logits` 常驻显存；新增 recipe/CLI 字段 `lm_head_chunk_size`，把最终解析值写入 checkpoint training_config 与 run metadata，resume 默认沿用记录值，不进入 `ModelConfig`。当前 dtype、loss 后端和分布式执行计划通过 dense/chunked 等价准入后，允许在完整 optimizer 提交边界显式覆盖 chunk size，但必须记录旧值、新值、原因和准入报告，静默变化 fail closed。chunk 只沿有效监督 token 维执行，loss 分母使用全局有效目标数；未来 TP vocab-shard loss 沿词表维分片，通过全局 log-sum-exp 和目标 token 分片贡献汇总，不得 gather 完整 logits，同一 token 集合的有效目标数在 TP group 内只计一次。补 dense、chunked、vocab-shard 的 loss、梯度和一步参数更新等价测试。
    - [ ] 2. 增加训练 `return_states=False`：训练 forward 不构造或返回推理用 `LayerStateV2`、RetNet/xLSTM detached state 和相关 dataclass；评测/推理保持原语义。
    - [ ] 3. 增加可开关 activation checkpointing：按 decoder block 或连续 block 分组重算激活，提供 `--activation-checkpointing` 与 checkpoint metadata 记录；默认关闭，长序列训练可开启。
    - [ ] 4. 优化 attention mask 与真实局部窗口执行：减少 dense mask 构造和长序列中间张量，评估局部 FlashAttention / block sparse / compressed sparse attention 作为后端候选。
    - [ ] 5. 调整 micro batch 与梯度累积策略：保持 `batch_size`、`gradient_accumulation_steps`、有效 token batch 的审计字段，确保小显存配置不改变 resume 和指标语义。
    - [ ] 6. 增加长度分桶与 packing 策略 benchmark：按样本长度分桶并比较顺序 first-fit 与确定性长度感知/BFD 候选，减少 padding 与短长混批波动；长度相同时使用稳定样本 id 或 ordinal 作为并列排序键，所有 `input_ids / labels / thinking/channel / position / segment` 字段使用同一装箱计划，报告 token 利用率、吞吐、显存峰值、样本顺序和质量变化。
    - [ ] 7. 建立 LongRoPE2 长上下文课程：预训练默认优先使用 32K 级上下文形成稳定基座，后续通过窗口采样、LongRoPE2 因子和专项长上下文阶段扩展到 1M 目标；每个阶段绑定独立评测与 checkpoint 元数据。
    - [ ] 8. 收紧训练 RoPE cache 上限：`train_rope_cache_max_sequence_length` 不得大于本阶段实际训练窗口；窗口混合训练时评估按窗口重建/复用 cache 的显存收益。
    - [ ] 9. 完成 bf16/fp16 混合精度审计：CUDA 在硬件原生支持时优先 bf16；fp16 保存并恢复动态 loss-scaler。记录参数、autocast、LM head、log-sum-exp 与 loss 累积 dtype，以及非有限值、溢出次数和 fallback 原因，禁止静默退回 fp32。
    - [ ] 10. 引入 Muon 优化器候选：新增 `optimizer=adamw|muon` 配置、参数角色分组、`state_dict` / checkpoint 恢复、学习率与 weight decay 口径和回归 benchmark；矩阵投影与 embedding/tied head、norm、bias、router、RetNet/xLSTM、LoRA 和其它 adapter 互斥分组，每个可训练参数恰好归属一次，共享参数按对象身份去重；Muon 作为 AdamW 的可选替代，不直接覆盖默认值。
    - [ ] 11. 评估 CPU optimizer offload：仅作为“训得起来优先”的低显存选项，明确标注吞吐损失；与 FSDP/ZeRO 路线解耦，不作为单卡默认路径。

- [ ] 7.4. 训练提交边界、RNG 与恢复治理
  - 目标：将可恢复 checkpoint 的提交单位冻结为完整 optimizer step，显式区分 `consumed_cursor` 与 `committed_cursor`。本项负责单机及各执行模式共用的 trainer-state、提交边界和通用 RNG 语义；P2 第 8 项只在此基础上扩展分布式 rank/world size/shard/sampler 与全 rank 共识，不另建第二套恢复状态源。
  - 未完成任务：
    - [ ] 审计并修正训练循环提交顺序：`global_step`、数据游标和 checkpoint 触发不得在 optimizer commit 前推进；梯度累积中间不得发布可恢复 checkpoint，所有周期检查点必须落在 committed optimizer 边界。
    - [ ] 冻结 `micro_step`、`optimizer_step`、`global_step` 的递增语义；中断发生在梯度累积中间时，首版丢弃未提交梯度并从最后 committed 边界重放；若保存中间态，必须同时保存全部未提交梯度、micro-step 和 scaler 状态。
    - [ ] 保存/恢复 Python、Torch CPU、各 CUDA、长度窗口采样、数据顺序、dataset shuffle buffer/iterator、packing 和数据采样等通用 RNG；后续新增随机源必须登记 RNG schema/version，连续训练与恢复训练随后若干 optimizer step 的样本、窗口、packing、loss、参数和 optimizer state 必须一致。分布式 rank 流的派生、分片与汇聚恢复由 P2 第 8 项扩展。
    - [ ] SIGINT/SIGTERM 只登记协调停止请求，signal handler、异常回调和结束回调均不得直接写入或发布 checkpoint；只能由训练循环协调者在已提交 optimizer 安全点完成唯一一次原子保存，避免异常路径与正常结束路径重复提交。Windows/Linux 使用同一停止语义；分布式全 rank 共识、超时和部分 rank 失效处理归 P2 第 8 项。
    - [ ] 修正 resume 后吞吐口径：使用区间 token 增量/区间耗时，并区分本次 run 与跨 resume 累计值。
    - [ ] checkpoint manifest 对关键文件按登记算法（默认 SHA-256）增加内容摘要校验，记录覆盖文件/字段、算法版本和 schema；摘要缺失、损坏或错配时 fail closed。

- [ ] 7.5. 纯文本批量分词与拒绝账本
  - 目标：批量分词提高转换吞吐，逐记录回退只用于定位失败，不改变正式训练样本集合。
  - 未完成任务：
    - [ ] 批量成功路径与逐记录路径逐 token 等价；批量失败时缩小范围或逐记录重试。
    - [ ] 为纯文本转换工具统一生成版本化、可机读的 rejection ledger；每条记录至少包含 `ledger_schema_version`、稳定 `sample_id` 或 ordinal、`source_digest`、`failure_stage`、稳定 `reason_code`、`retry_count` 和枚举化 `disposition`，并绑定 tokenizer/template/schema/manifest 指纹，不复制可能包含敏感内容的原文。
    - [ ] 转换程序以代码断言 `输入总数 = 接受数 + 拒绝数 + 显式策略过滤数`；accepted、rejected、filtered 集合按稳定样本身份两两不相交且并集等于输入集合，ledger 行数、原因码计数与 rejected 集合一致；批量、逐记录定位和 resume 路径产生相同集合指纹。
    - [ ] 在版本化数据转换配置或 manifest 引用的 preprocessing policy 中登记 `reason_code_schema_version`、`max_rejection_rate`、拒绝率分母和显式过滤规则，并把策略 id、版本和摘要写入 source manifest/run metadata；该策略不进入 `ModelConfig`。正式转换缺少策略、遇到未登记错误或超过阈值时 fail closed，显式策略过滤不得伪装成拒绝。

- [ ] 7.6. WSD 可选学习率调度
  - 目标：为长周期 `text_pretrain` 提供 warmup/stable/decay 三段式可选 recipe，不改变当前默认调度。
  - 成功标准：阶段预算、当前阶段、已消耗 token/step 和恢复状态全部写入 trainer state；与 cosine 在相同 token、数据、seed、峰值学习率下完成对照，短期 SFT/LoRA 不启用。

- [ ] 7.7. 纯文本模型初始化与稳定性组合准入
  - 目标：在不改变当前纯文本默认拓扑的前提下，将技术规范成熟、语义兼容的深度缩放残差初始化、Q/K 归一化、Attention 输出门控和训练期 embedding 噪声按依赖组成版本化稳定性候选包，先执行统一质量、稳定性与成本测速；发生回归时再定向拆分定位，不做排列组合式穷举。
  - 未完成任务：
    - [ ] 明确从头训练与已有 checkpoint 派生两种初始化口径，记录 `init_policy_version`、总层数、残差分支数量、初始化统计和随机种子；派生 checkpoint 时不得重新初始化已有权重，禁止将未经来源和单元测试审计的公式或常量写入默认配置。
    - [ ] 冻结统一基座：可由 checkpoint 派生的候选以训练后的 `artifacts/lpt_v2/text_pretrain`、chat SFT workflow 和同预算 base-continued 为基线；必须从头生效的初始化策略使用相同 text-pretrain recipe、数据、token 预算、seed、LongRoPE2 和硬件另建等价基线，不得把两种口径混为一条派生链。
    - [ ] 为 Q/K 归一化和 Attention 输出门控保留可独立关闭的开关及等价 reference 路径，用于回滚、故障定位和不兼容项隔离；候选包统一报告梯度范数、激活范围、loss 曲线、长上下文指标和吞吐/显存变化。
    - [ ] 评估 embedding 输入噪声的注入位置、分布、幅度/缩放规则、训练阶段和关闭条件；冻结 seed 派生、padding/packing mask 以及控制向量是否扰动，eval 必须关闭；不得污染标签、控制通道或 checkpoint 恢复状态。
    - [ ] 候选包与基线使用相同数据、token 预算、峰值学习率、seed 和评测集；统一准入同时覆盖质量、稳定性、资源和恢复兼容性。只有出现回归、指标归因不清或技术互斥时，才依据观测指标和依赖图执行定向关闭、二分拆包或单因素诊断；不得为穷举所有开关组合而增加训练分支。

- [ ] 7.8. 条件专项：版本化预分词数据分片
  - 触发条件：现场 tokenizer 成为长周期 text pretrain 的可测瓶颈，且数据规模足以覆盖索引/缓存维护成本。
  - 成功标准：预分词分片拥有版本化 schema、tokenizer/template/manifest 指纹、边界与校验信息；它只能作为可重建加速缓存，不能与原始 manifest 形成两个权威样本集合，损坏或指纹错配时回退到显式重建并 fail closed。

## P2：旧 P2 部署与治理任务的 v2 迁移

- [ ] 8. 分布式执行接入层 v2（训练 + 多卡推理）
  - v2 目标：统一模型加载、设备映射、推理 session、训练分布式入口和运行报告，让单卡、多卡推理与训练共享一致的执行配置语义。训练恢复复用 7.4 冻结的通用 trainer-state，本项只负责 rank/world size/shard/sampler、分布式 RNG 派生和全 rank 协调语义。
  - 已完成部分：`lpt_runtime/execution.py` 已提供 `ExecutionConfig / DeviceMapPlan`、`CUDA_VISIBLE_DEVICES` 感知、逻辑 GPU 发现、`single / auto / model_parallel` 推理计划和保守层级模型放置；`tensor_parallel / fsdp` 已作为显式未实现模式保留。
  - 未完成任务：
    - [ ] 真实多卡推理 smoke：在 `CUDA_VISIBLE_DEVICES=0,1` 下跑通同一 v2 checkpoint 的 `model_parallel --device-map auto`。
    - [ ] 推理侧完善 Model Parallel：自动 device map 同时考虑参数量、Paged KV 预算、RetNetAssist/xLSTM state bytes 和显存余量。
    - [ ] 推理侧跨卡状态验证：确认 `InferenceSession / LayerStateV2 / Paged KV / Assist state` 在跨卡放置下稳定，并输出每卡显存、吞吐、首 token 延迟和总延迟。
    - [ ] 算子级 Tensor Parallelism：设计策略对象，拆分 Attention 的 `q/k/v/o_proj`、MoE/SwiGLU 的 `w1/w2/w3` 与 RetNetAssist 投影层，并加入单卡 vs TP logits 近似一致性测试。
    - [ ] 训练侧 FSDP2：增加 `rank / local_rank / world_size` 配置、`torch.distributed` 初始化/销毁、`torchrun` 启动说明和 block 级 wrapping 策略。
    - [ ] 分布式状态治理：在 7.4 通用提交边界和 RNG schema 上保存/恢复 rank/world size、分布式 RNG 派生、optimizer/scheduler shard metadata 与 sampler 分片状态，并与 checkpoint schema v2 对齐；不得重复定义 `global_step`、`optimizer_step` 或数据游标事实源。
    - [ ] 执行器职责收敛：由分布式执行适配层统一负责 wrap、`no_sync`、backward、全局梯度裁剪、state gather 和 rank-0 副作用；optimizer 必须在正确 wrap 时点后创建。
    - [ ] sampler 精确恢复：保存 epoch、已提交 ordinal、global/local cursor、seed、rank、world size、drop policy、manifest fingerprint 以及长度窗口/packing RNG；world size 变化首版 fail closed，显式重分片必须单独验证，禁止静默重放或丢样本。
    - [ ] 分布式协调停止：信号只转换为 stop request，所有 rank 在安全 optimizer 边界达成共识后原子保存；记录超时、部分 rank 失效、终止原因和清理结果。
    - [ ] Attention execution plan 治理与非分页接入：统一 training、prefill、decode、paged decode 的能力描述、选择/fallback 规则和报告口径，并让计划真实驱动 training、prefill 与普通 decode；实际后端和 fallback reason 写入报告，reference path 与优化后端完成 logits/梯度/贪心序列等价准入。P3-KV2 负责 Paged KV 描述、block table 和 direct paged decode 的具体执行接入，不在本项重复实现页表数据面。
    - [ ] 执行配置治理：checkpoint、评测报告和 benchmark 记录 `execution_mode`、`CUDA_VISIBLE_DEVICES`、逻辑 device map、world size，且不绑定物理 GPU id。
    - [ ] 验证准入：补单卡推理回归、多卡长上下文生成 smoke、FSDP2 单机多卡最小训练 smoke，并将显存/吞吐/延迟报告落盘。

- [ ] 9. 服务化推理入口 v2
  - v2 范围：在当前 CLI、`InferenceSession`、checkpoint loader 和执行计划基础上补本地 HTTP API 或进程内 server wrapper。
  - 成功标准：同一 v2 checkpoint 可通过 CLI 与 HTTP 完成非流式和流式生成；服务层不直接耦合单卡/多卡执行细节。结构化输出、流式 token 事件和 Function Call 的核心协议先作为 `lpt_inference / lpt_protocol` 原生能力提供，服务层只负责 API 映射、鉴权、会话和传输。
  - 原生能力边界：
    - [ ] tokenizer 感知的有状态增量解码：decoder state 绑定 request id、生命周期代次和 channel，保存待定 token/byte/text、stop matcher 前缀状态、最后已提交边界及 flush/terminal 标记；分别维护不完整 UTF-8、BPE/byte fallback 和空白合并状态，定义非法字节处理，保证所有流式 delta 拼接等于最终整体 decode，并在结束时执行幂等 flush。
    - [ ] 跨 token stop matcher 与终态账本：冻结已登记 tokenizer 解码、特殊 token/通道过滤、byte fallback、非法字节及 flush 规则产生的最终可见 answer 文本标量流为匹配视图；首版不额外执行 NFC/NFKC、大小写折叠或空白规整，按 Unicode 标量值精确匹配。停止串校验、`max_stop_sequence_length` 和 `stop_holdback_capacity=max(0, max_stop_sequence_length-1)` 必须使用同一标量值单位，byte pending 与文本 holdback 分开维护。测试覆盖中文、多字节字符、辅助平面 Emoji、组合字符、跨 token/byte 边界、非法字节，以及规范等价但标量序列不同的文本，验证默认精确模式不把预组合与分解形式误判为相同停止串；停止串及其前缀不得泄漏，流式拼接必须与一次性解码一致。冻结 EOS 与 stop 同时命中的优先级，终态唯一并区分 `eos / stop_sequence / length / cancelled / error / tool_call`，同时记录 sampled token、emitted token、thinking/answer 通道计数和客户端可见字符数。
    - [ ] 请求生命周期原生契约：事件绑定 request id、生命周期代次和单调序号；取消、超时、异常、消费者提前退出和 reset/release 幂等，终态后禁止继续输出 delta，单请求失败不得终止其它 row。设备上下文或页池进入永久 quarantine 时，服务 readiness 必须 fail closed；只有 P3 新页池代次通过启动自检后才能恢复流量，旧请求必须进入明确错误终态或走显式重建/重放，不得静默续用旧 session、page 或 lease 引用。
    - [x] 结构化输出：推理配置支持 `output_format=text|json|tool_call`；JSON 输出需有解码期语法约束和解析状态，便于系统集成。
    - [x] 流式输出：`lpt_inference` 提供可复用的 token/event generator，`main.py --stream` 先落地 CLI 实时输出，后续 HTTP SSE 直接复用同一事件流。此处只表示事件通道可用；tokenizer 感知增量解码、跨 token stop 和终态正确性仍以本节上方未完成项为准。
    - [x] Function Call：扩展 chat schema，允许 assistant 原生 `tool_calls=[{id,name,arguments}]`，训练、推理和返回结果都使用结构化字段，不用自然语言标签模拟工具调用。
    - [ ] MCP：留在服务化阶段实现。MCP client 读取外部 MCP 工具与数据源，转换成原生 Function Call tool schema；模型只感知“可调用工具”，不绑定 MCP 协议细节。
  - 2026-05-24 当前结果：已完成 `GenerationConfig.output_format / tool_choice`、JSON object/array 前缀约束解码、结构化解析结果、`GenerationStreamEvent` 流式事件、`main.py --stream`、`main.py --tools-json`、assistant `tool_calls` schema 校验与训练渲染。Function Call 当前只提供原生结构、解析和回填能力，不执行外部工具；MCP 与真实工具执行留到服务化阶段。
  - 未完成任务：
    - [ ] OpenAI 兼容 `/v1/chat/completions`。
    - [ ] Anthropic 兼容 `/v1/messages`。
    - [ ] SSE 流式输出，复用 `lpt_inference` 原生流式事件。
    - [ ] MCP 工具接入：服务层作为 MCP client，完成 tool schema 映射、调用执行、结果回填和错误治理。
    - [ ] 会话新建、续写、重置、释放和最大上下文/生成长度限制。
    - [ ] 实现并测试单请求采样与 logprob 语义：冻结单请求 API、采样顺序、RNG、终态账本和返回字段；保存 temperature、top-k/top-p、惩罚、seed/RNG、结构化约束和终止状态，冻结 `raw_logprob=log_softmax(raw_logits)[chosen_token]` 与 `behavior_logprob=log_softmax(behavior_logits)[chosen_token]` 两个字段。测试覆盖贪心、temperature、各类惩罚、top-k/top-p、grammar 及组合顺序，被过滤 token、EOS/stop 同步命中、隐藏 thinking、可见 answer 和截断 token 均与独立 reference 公式对照，流式与非流式口径一致。P3 只把同一语义扩展到 continuous batching 的 row 维度。
    - [ ] 保守同步或小批量调度；continuous batching 留到 P3 评估，服务层先验证 chunked prefill、背压和单 row 故障隔离契约。
    - [ ] 服务指标：请求延迟、排队时间、首 token 延迟、TPOT、tokens/sec、输入/输出 token 数、sampled/emitted 字符计数、显存占用和错误率；指标标签禁止使用 request id、prompt 摘要等高基数字段。

- [ ] 10. 实验治理 v2
  - v2 范围：将配置快照、manifest、checkpoint、评测结果、资源报告、tokenizer metadata、chat template、执行计划、候选包、统一基准套件和 git commit 绑定到同一实验目录。
  - 已完成部分：训练 trainer state 已保存 `source_manifest / eval_manifest / tokenizer_metadata / training_config`；checkpoint 已保存完整 `model_config` 和 runtime metadata。
  - 未完成任务：
    - [ ] 定义 v2 实验目录 schema，覆盖 `config/`、`checkpoints/`、`reports/`、`metrics.jsonl`、`run_metadata.json`。
    - [ ] 所有训练、评测、benchmark 工具统一写入 tokenizer metadata：`tokenizer_path`、`tokenizer_name_or_path`、`vocab_size`、tokenizer 配置指纹、BOS/EOS/PAD token 与 id、`chat_template_version`。
    - [ ] 报告绑定 manifest 摘要、数据指纹、checkpoint 摘要、profile、preset、attention/cache backend、execution mode 和 git commit；候选技术另外登记 `spec_id / source_type / source_locator / source_revision / license_id`，使规范标识、公开来源、版本和许可不依赖任何过程材料。
    - [ ] 定义版本化候选包 manifest：至少登记 `candidate_package_id / candidate_package_version / lifecycle_stage`、组件及其 `spec_id`、依赖与互斥关系、完整配置 diff、reference/fallback、回滚顺序、适用 profile/硬件和基线 checkpoint/recipe；manifest 内容摘要是正式运行身份的一部分，禁止只在报告正文中自由描述组合。
    - [ ] 定义版本化统一基准套件 manifest：至少登记 `benchmark_suite_id / benchmark_suite_version`、eval manifest 与摘要、工作负载、指标名称/单位/比较方向、有限准入门槛、统计方法、正确性与恢复用例、资源测量口径，以及要求的硬件、dtype 和 execution plan；组合候选与定向诊断必须复用同一套件版本，诊断性删减另记 override，不能静默改变基准。
    - [ ] checkpoint manifest 对关键文件按登记算法（默认 SHA-256）增加内容摘要校验；实验目录记录覆盖文件/字段、算法版本、optimizer 类型、参数组 schema、RNG schema/version、实际执行后端和 fallback reason，元数据缺失或内容错配时 fail closed。
    - [ ] 训练和推理吞吐使用区间 token/时间，分别报告本次 run 与跨 resume 累计口径；在线指标只使用低基数标签。
    - [ ] 条件专项：独立推理权重导出。仅允许从严格校验通过的 v2 checkpoint 派生纯张量格式，保存架构/模型配置、tokenizer、dtype、量化和执行计划元数据；导出物必须可逆校验、可拒绝错配，不能成为训练恢复或模型配置的第二权威来源。
    - [ ] 跨 tokenizer 对比显式区分“同源可比数据”和“历史参考数据”。
    - [ ] 缺少旧 tokenizer 绑定 checkpoint 时，不允许用新 checkpoint 强行复跑旧 tokenizer 基线。
    - [ ] 每次正式 run 在 `run_metadata.json` 绑定候选包与基准套件的 id、版本和内容摘要，并增加实验目录校验工具；实际配置与候选包 diff 不一致、报告未覆盖套件必需项、manifest 缺失或摘要错配时 fail closed。

## P3：v2 架构与推理扩展评估

- [ ] 11. 评估 v2 架构与推理扩展
  - 已完成部分：Paged KV、SwiGLU-MoE、Shared RetNetAssist Q Adapter、xLSTMAssist 基础、状态池、衰减、边界重置和资源报告已经落地。
  - 治理裁决：2026-08-02 前已立项并完成的 22-27 单项分支用于历史归因，保留原方法和结论，不追溯改写为组合实验；2026-08-02 后新增或尚未启动的兼容候选遵守“组合优先、统一测速、回归后定向拆分”。互斥配置、机制实现正确性、参数定型和回归诊断仍可保留单项 reference，但不得据此恢复全排列实验。
  - 任务状态：
    - [x] 评估 xLSTMAssist Memory Gate。已完成第 22 项三分支 chat SFT 对比、独立 eval manifest 的完整 1164 step 补充实验和实验报告；结论为放弃当前输入 Memory Gate 方案，暂不默认启用。
    - [x] 评估 xLSTMAssist 记忆粒度：第 23 项五分支 1164 step chat SFT 对比已完成，所有分支通过 checkpoint validate、真实 checkpoint forward smoke、长上下文代理、资源和 xLSTM 专项评测；结论为 `every_4_layers` 作为后续组合实验中的 xLSTM 记忆粒度候选，`every_2_layers` 保留候补对照，`all_layers` 和 `selected_late_layers` 不进入后续组合实验。local/global memory 待 forward 路径接入后单独评估。
    - [x] 评估 K Adapter：第 24 项 `q_adapter` 与 `qk_adapter` 对比已完成；Q/K 分支 eval loss 更低，但长上下文代理和 router z-loss 退化，因此不进入默认主干或组合候选，只保留归档对照。
    - [x] 评估 RetNetAssist 参数与状态共享策略：第 25 项 global/group/per-layer 对比已完成；`global/per_layer` 作为组合候选，`global/group` 作为低状态成本 fallback，其余方案不进入默认主干。
    - [x] 评估 RetNetAssist 启用层与 rank：第 26 项启用层与 rank 对比已完成；`every_4_layers/rank16` 作为组合主候选，`every_2_layers/rank16` 保留质量备选，rank32 不进入默认主干。
    - [x] 评估 RetNetContextAdapter：第 27 项已完成；长上下文代理收益明确，作为长上下文组合主候选，最终是否进入默认主干由组合候选统一准入决定。
    - [ ] 完成 CLA 共享实现与评估：首个候选为 `cla_share_every_n_layers=2`，比较吞吐、显存、质量与状态成本。
      - 当前代码状态：`cla_share_every_n_layers` 只是禁用态保留字段，`ModelConfig` 默认并强制其为 `1`，值不为 `1` 时直接拒绝；`LPTV2` 每层 `LocalAttentionMixerV2` 独立创建 `k_proj/v_proj`，模型构造和 forward 没有消费该字段。当前只有拒绝 CLA 启用值的配置测试，没有 CLA 启用路径的参数共享、跨层 K/V 复用、缓存/checkpoint 测试或实验报告，因此不得标记为已实现或已评估。
      - 实现准入：先冻结“共享投影参数”与“复用跨层 K/V 激活/cache”的准确语义、分组 leader/follower、梯度流、Local Attention/LongRoPE2 位置关系，以及 Paged KV 的页归属、alias/refcount/reset/rebuild；补配置、参数对象身份、forward/backward、checkpoint、参数统计和缓存生命周期测试。
      - 采用准入：`cla_share_every_n_layers=1|2` 是互斥配置，先完成 `1` reference 与 `2` correctness/cost 对照；实现正确性通过后，CLA 才能作为组件进入相应运行时候选包并复用 P2-10 统一基准套件，不与其它开关做全排列。
    - [ ] KV0 语义基线冻结：建立 dense、当前轻量 Paged KV 和未来物理页池的逐 token logits 等价矩阵；冻结 Local Attention、LongRoPE2、prefill/decode、reset/rebuild、RetNet/xLSTM 边界以及 prompt 最后一个 token 只处理一次的语义。
    - [ ] KV1 单请求物理页池：固定容量物理 K/V pool、request block table、稳定 slot、generation 句柄、增量尾页写入和窗口滚动回收；分别实现按 `request_id + generation` 管理冻结预算、资源稳定引用及 `reserved|committed|rolled_back` 状态的请求级资源预留，以及保护在途设备访问的 GPU 执行 lease。资源预留 commit 后所有权转移给 request/session；lease 至少绑定 `lease_id`、请求与代次、资源稳定引用、device/execution context、完成 fence/event、取得时间、deadline/timeout policy、`active|expired|quarantined|released` 状态和释放原因，并冻结先取得租约再提交设备、设备完成可证明后才释放的迁移规则；当前轻量页池继续作为 CPU/reference 基线。
    - [ ] KV2 Attention 后端接入：复用 P2 第 8 项冻结的 execution plan、能力矩阵、选择/fallback 和报告口径，实现 Paged KV 描述、block table 对 materialize/reference 与 direct paged decode 的真实驱动；覆盖页表、有效偏移、GQA、Local mask、不同 page size 和能力不足时的确定性降级，显式固定后端不可用时 fail closed；对 `reserve/map/prefill/decode/release` 各阶段加入故障注入和不变量恢复测试，并覆盖 lease 取得、设备提交、completion fence、超时隔离与 release 的交错顺序，验证未取得租约不得提交、fence 未完成不得重新分配物理页。
    - [ ] KV3 continuous batching：拆分清理、准入、chunked prefill、decode、补位和 terminal 队列；按 token、KV 页、RetNet/xLSTM state、临时空间和设备余量预算，加入公平性、背压、取消、分配失败回滚和单 row 错误隔离。
    - [ ] KV4 安全 prefix sharing：完整身份、链式前缀、摘要命中后的精确比较、Assist 边界状态等价；完整页只读共享，尾页默认独占，共享尾页必须 COW 或等价隔离，LRU 只能驱逐零活跃引用的封存缓存页。
    - [ ] KV5 后置能力：评估 page 级量化、preempt/resume 和多模态 anchor/rolling 两区复用；抢占恢复后的 token 序列、逐 token logits、RetNet/xLSTM 边界状态和终止原因必须与不中断参考路径一致；首版不实现自动 CPU/磁盘换页、透明 page fault 或无协调活跃页驱逐。
    - [ ] KV 设计决策与 page size sweep：在进入 KV1 前冻结跨层物理 slot、模型并行 block table、准入预留、部分头页、CPU reference、prefix sharing 范围、LongRoPE2 阈值重建原子性、两区共池和执行租约等决策；lease 超时只触发取消、告警和资源隔离，必须等 completion fence 完成或 executor 明确确认设备不再访问后才能回收，状态不明时 fail closed，禁止按墙钟强制复用物理页。补充永久 quarantine 恢复决策：确定性销毁旧 executor/device context 或进程重启后，从空状态建立新页池代次并拒绝全部陈旧 page/lease 引用；请求只能明确失败或从已提交历史经 reference prefill 重建，P3 负责页池代次与启动自检，P2 负责 readiness、终态和显式重放。用代表性负载比较候选 page size 的内部碎片、映射表字节、分配/回收延迟、有效占用率和峰值显存。
    - [ ] 逐 row 采样与 logprob：直接复用 P2 第 9 项已经冻结的单请求采样顺序、字段和计数口径，只增加 continuous batching 中每个 row 的独立参数、结构化约束、seed/RNG、终态、token 计数与故障隔离；不得实现第二套 sampler 或重新定义 `raw_logprob / behavior_logprob`。
    - [ ] 评估 prefix sharing 与 continuous batching 的完整性能/正确性报告，覆盖 TTFT、TPOT、吞吐、P95/P99、排队/补位/回滚/命中/驱逐和低基数指标标签。
    - [ ] 评估低秩统一状态 / MLA 类压缩。
    - [ ] 在具备依赖、CUDA 版本和 GPU 架构的 Linux 环境中评估 FA2/FA3 原生 Attention 后端。

- [ ] 12. 条件触发：完整 LongRoPE2 搜索器 v2
  - 触发条件：模型在 2K / 4K / 8K 上已有稳定训练 checkpoint baseline，且 7.1 的候选因子 sweep 证明因子选择显著影响 needle / PPL / retrieval 指标。
  - v2 范围：候选因子搜索、needle/PPL/retrieval 目标函数、多轮选择、搜索报告落盘，以及最终因子作为可审计配置产物写入实验目录。
  - 成功标准：`longrope2_long_factors` 从 bootstrap 或手工导入升级为可复现、可审计的搜索产物；checkpoint 不被隐式修改。

## P4：核心架构与工程优化评估专项

- [ ] 13. 建立核心架构与工程优化评估矩阵
  - 资料边界：每个候选技术必须具备版本化技术规范、LPT v2 当前实现基线和可复现实验；规范来源限定为带版本标识的公开论文、官方技术文档/模型卡，或许可证允许项目使用且经过审计的公开实现。实验报告和 run metadata 必须登记 `spec_id / source_type / source_locator / source_revision / license_id`，并同时固定公式/配置版本、基线 checkpoint、数据与资源口径。
  - 归档原则：本项保留两类内容：一类是会直接影响 LPT 模型结构、泛化能力、长上下文能力、推理能力、训练目标或后训练能力的核心改动；另一类是能支撑这些核心改动落地的工程优化，例如量化、融合 kernel、重算和 benchmark。Muon 优化器留在 P1.5，不在 P4 重复；纯服务/API 映射不放入本项。
  - 采用与测试原则：训练提交、状态回滚、解码正确性和拒绝账本等正确性约束直接实施，不进入架构消融；技术规范成熟、依赖满足且语义兼容的候选按依赖组成版本化候选包，与冻结基线执行一次统一的质量、稳定性、资源、恢复和硬件实测。出现回归或归因不清时才做定向关闭、二分拆包或单因素诊断；互斥架构、破坏 schema 的替代路线、缺少目标硬件实测的 kernel/低精度路径继续隔离验证，不做全排列组合。
  - 任务清单：
    - [ ] 1. Hybrid Attention 核心结构：完成接口与状态兼容性审计后，将可共存的滑动窗口、压缩 KV、稀疏 top-k 检索与现有 Local Attention/Paged KV/RetNetAssist/xLSTM 纳入版本化运行时候选包；占用同一语义槽位的替代结构保持互斥。固定窗口、层间配比和检索预算，记录 KV bytes/token、单 token FLOPs、TTFT/TPOT、质量指标与显存峰值，并提供 reference/fallback 路径；未完成统一准入前不得替换默认 Attention。
    - [ ] 2. MLA / 低秩 Attention 投影：验证低秩 Q/O/KV 投影对参数量、激活和状态存储的影响；必须实际测量 latent KV cache bytes/token、decode wall-clock 和重建误差，禁止在缓存前展开完整 K/V 后宣称压缩收益；与当前 GQA、RetNetAssist 及普通低秩基线做同预算对照。
    - [ ] 3. mHC 架构实验：将残差混合封装为可关闭、可回退的 block 候选组件，在兼容的架构候选包中统一报告归一化/约束信号、梯度传播、激活范围、显存、通信与 kernel 成本；仅在组合回归时对最短怀疑链做小规模稳定性和泛化复测，不把某个未审计公式或迭代次数直接写入默认路径。
    - [ ] 4. MoE routing：将兼容的 shared expert、routed expert、门控函数、top-k bias、早期层路由、容量因子、overflow 策略与负载辅助项组成版本化路由候选配置，同一语义槽位只选择一种机制；明确 top-k bias 是否只参与选择、路由权重如何归一化，以及 load-balance/z-loss 是否真正进入 LM 总 loss（不得只记录 detached 指标），统一报告 expert utilization、丢弃率、通信/显存和质量，覆盖 TP/单卡语义；回归后再定向关闭组件定位。
    - [ ] 5. FP4 + FP8 混合量化工程优化：按目标硬件和真实 kernel 能力组成包含 block-wise scale、FP8 激活、FP4 expert 权重、非 RoPE 维 Hadamard rotation、power-of-two scale 的版本化低精度 profile；训练期 fake quant/QAT 与真实低精度执行口径分开记录。报告校准集、scale/zero-point、溢出处理、算子 fallback、端到端质量/吞吐/显存，不得将仅 fake quant 的结果当作真实硬件收益。
    - [ ] 6. MTP 多 token 预测训练目标：将辅助 head/block、主自回归 loss 与事务化草稿生成作为同一 MTP 候选能力统一测量数据效率、草稿接受率、额外显存、错误回退和关闭后的兼容性；冻结 draft/target 的 token 对齐、mask、logprob 与外层 `checkpoint_schema_version=2` 兼容口径，草稿接受/拒绝必须事务化，拒绝 token 不得永久改变 Paged KV、RetNet/xLSTM、控制态或终态，接受时只提交一次，stream/logprob/终态账本必须与最终提交序列对齐。
    - [ ] 7. 长上下文泛化路线：LongRoPE2 仍是 LPT 默认位置编码与窗口课程；对其它扩展方法只做独立版本化对照方案或独立实验，不得隐式混入默认配置。所有候选都须经过窗口课程、factor sweep、needle/PPL/retrieval、训练稳定性和显存预算准入。
    - [ ] 8. Native thinking 上下文治理：定义 thinking/answer/tool 通道边界、历史保留优先级和按 token 的裁剪规则，裁剪只能发生在语义边界，删除 token 后必须重建或恢复对应 K/V、RetNetAssist 和 xLSTM 边界状态；验证多轮推理、工具调用、停止串、结构化输出和 tokenizer 一致性，禁止按字符粗暴截断或把内部思考泄露到可见通道。
    - [ ] 9. 后训练 / RL 核心能力：单机小规模 token-id rollout 必须调用 `InferenceSession`，或调用与其共享生命周期、资源所有权和采样契约的正式 executor 核心，不得建立第二套 sampler、KV cache 或状态路径；以 GRPO smoke、行为策略/原始策略 logprob、KL 修正、奖励异常和回滚为准入项，固定 reference checkpoint、采样配置、奖励版本、轨迹账本和恢复点，先验证循环正确性再比较推理质量，不与 Muon optimizer 混写。
    - [ ] 10. 单机内核与重算工程优化：评估 PyTorch/Triton/TileLang 等可选 fused kernel、selective recompute 和 deterministic batch-invariant kernel；每个 kernel 必须有 reference fallback、边界/异长/GQA/量化测试、编译与设备能力记录、峰值显存/吞吐/质量报告，不能以“可编译”代替端到端准入。
    - [ ] 11. 条件专项：离线 DPO 阶段。单独冻结 chosen/rejected 数据契约、response mask、reference checkpoint、策略/参考模型 logprob、温度与 KL 口径、奖励版本和回滚点；纯离线 DPO 不强制生成 rollout，但策略与参考模型 logprob 计算必须复用正式模型前向及同一 token、response mask 和已冻结 logprob 契约，不得建立第二套 forward/logprob 路径。偏好数据若由在线采样生成，另行记录 raw/behavior sampling logprob、采样配置及来源；若进行在线数据构造或 rollout 评测，则继承 P4 第 9 项的正式 executor、采样、KV 和状态契约。先完成恢复与审计 smoke，再与同 token 预算的 SFT 对照，不能把自动排序样本直接当作偏好对，也不能改变默认三阶段 workflow。
  - 2026-08-02 采用结论（覆盖此前的默认单变量实验顺序，不改写已完成的历史实验）：
    - 残差混合、压缩 KV、稀疏路由、低精度量化、MTP 和后训练各自可能带来收益，但均有额外状态、验证和硬件依赖；不能凭单项离线指标直接进入默认主干。
    - 评估顺序：先完成可复现 reference、状态/损失语义、候选包依赖清单和成本基线，再将成熟兼容项组合接入并统一测速；只有统一准入出现回归、候选互斥或指标无法归因时才定向拆分，不执行排列组合式对比。质量、稳定性、资源、恢复兼容性和所需硬件实测同时达标后才形成候选 recipe。
    - P4 的产物是独立的实验矩阵、准入报告和可回滚配置，不是对当前 LPT 默认架构的隐式替换。

## P5：多模态核心能力专项

- [ ] 14. 建立 LPT v2 原生图像理解与多模态训练闭环
  - 边界：P5 架构固定为 `SigLIP2-NaFlex 视觉编码器 + LPTV2 文本/语言主干 + projector/token reducer 连接层`；默认视觉塔固定为 `google/siglip2-so400m-patch16-naflex`，P5 不规划其他视觉塔路线，也不替换 `LPTV2` 作为文本编码器或语言模型主干。SO400M 的 1152 专指该视觉编码器输出的特征嵌入维度；按 patch16、NaFlex 多分辨率/保留宽高比和可配置的单图/单样本视觉预算规划，实际 raw token 数由 processor/batch 记录，进入 LPT 的 token 数受 reducer 和样本总预算约束，具体预算由 MM0 实施方案确定。实现必须保留 `LPTV2`、`ModelConfig`、v2 checkpoint、native thinking、manifest/data_progress 和三阶段 workflow 语义，不引入外部 MLLM 主干、双塔检索主线或自然文本 `<image>/<think>` 控制协议。
  - 内部阶段映射（属于第 14 项的串行准入门，不新增独立项目范围）：
    - [ ] 14.1 / MM0 契约冻结：`model_config_schema_version=4`、`multimodal_schema_version=1`、数据 schema v1、派生规范、eval manifest 量化门槛字段、`inputs_embeds` 路径设计与等价测试、训练数据规划（样本量/配比/replay/拒绝策略、完整轮次子样本与权重）、依赖资产清单和可复现性要求、预算 smoke 与早期基线、请求生命周期/事件、多图反事实与合法同轮布局契约、任务同步和纯文本不变约束测试计划；外层 `checkpoint_schema_version=2` 保持不变。
    - [ ] 14.2 / MM1 特征前向：假视觉塔、真实 image processor 三字段契约、reducer/projector、结构化模态分发、序列装配、`inputs_embeds`、label/mask/梯度测试。
    - [ ] 14.3 / MM2 固定媒体 KV：两区 Paged KV、超窗口可见性、anchor/rolling 生命周期、generation/lease、preempt/resume 和 session 重建；恢复后 token、logits、状态边界、引用计数和终态必须与不中断路径一致。
    - [ ] 14.4 / MM3 图像训练：align/SFT/LoRA recipe、feature cache、拒绝账本、有效媒体行执行、禁止零值伪梯度、Assist/decay/router 兼容组合包与回归定向诊断矩阵和 replay 闭环。
    - [ ] 14.5 / MM4 图像准入：请求生命周期/终态/清理、逐请求失败隔离、多图绑定/顺序/future-media 反事实、合法同轮图文布局鲁棒性、质量/安全/性能报告、P0 真实 checkpoint 长图文、纯文本回归和成本门槛。
  - 模型任务：
    - [ ] 1. 多模态配置与 schema：新增 `multimodal_enabled`、`multimodal_schema_version`、`vision_encoder_name_or_path=google/siglip2-so400m-patch16-naflex`、`vision_encoder_family=siglip2_naflex`、`vision_hidden_size=1152`、`vision_patch_size=16`、`vision_max_num_patches`、`vision_token_budget_per_image`、`vision_token_budget_per_sample`、`vision_projector_type`、`vision_token_reduction`、`mm_attention_mode`、`media_attention_policy` 等字段，并严格升级 `ModelConfig` schema；`vision_raw_num_tokens` 只记录在 batch/metrics/report，不进入静态配置；媒体使用结构化 span 和 `inputs_embeds`，不引入 `image_slot_token_id` 或用户可见媒体哨兵。
    - [ ] 2. 视觉塔与投影器：新增视觉模块，仅加载视觉子模型，不加载 SigLIP2 文本塔、tokenizer 或完整双塔；图像预处理使用与视觉塔配套的 image processor，消费其返回的 `pixel_values / pixel_attention_mask / spatial_shapes` 并转换为 LPT 的 `image_features / image_token_mask`；其中 1152 是 SigLIP2 SO400M Patch16 NaFlex 输出的视觉特征嵌入维度，加载时必须与实际 encoder config 严格校验；不使用 pooled image embedding 作为全部视觉输入；默认冻结视觉塔，使用 `1152 -> hidden_size` MLP projector，保留 projector 类型、token reducer 和低学习率顶层解冻作为后续实验项。
    - [ ] 3. LPTV2 早期融合前向：增加 `pixel_values / image_features / image_token_mask` 输入，把视觉 embedding 与文本 embedding 对齐后融合；同步维护 `attention_mask / position_ids / segment_ids / thinking_mode_ids / target_channel_ids / modality_ids`，视觉 token 的训练 label 固定为 `-100`。
    - [ ] 4. 视觉前缀 attention 策略：MVP 不引入 cross-attention；必须提供 `media_attention_policy=pinned_media_kv|local_window_only`，并基于 `image_token_mask` 处理 NaFlex 变长视觉 token，避免视觉前缀在 Local Attention / Paged KV 滑窗中被意外裁掉。MVP 只准入 `mm_attention_mode=causal_prefix`；`vision_bidirectional` 仅保留 schema 枚举和 Post-MVP 互斥模式对照，不阻塞图像闭环。
    - [ ] 5. 状态与路由治理：让 RetNetAssist、xLSTMMemory 和 MoE router 能读取 `modality_ids` 或等价 mask，评估视觉 token 独立 assist 权重、memory decay 和 router bias，避免稀释文本记忆或造成 expert 负载偏移。
  - 工程任务：
    - [ ] 1. 数据协议与 manifest：新增 `multimodal_chat`，支持 `content_parts`、图像列表、多轮 messages、assistant thinking 和 tool_calls；图像默认用 `source_type=path`，base64 仅作为 API/调试/小样本补充；继续按 JSONL 文件粒度维护 `weight / sample_limit / data_progress.json`。
      - 草案样例：

      ```json
      {
        "schema_version": 1,
        "type": "multimodal_chat",
        "messages": [
          {
            "role": "user",
            "content_parts": [
              {"type": "image", "image_id": "img0"},
              {"type": "image", "image_id": "img1"},
              {"type": "text", "text": "比较这两张图片。"}
            ]
          },
          {
            "role": "assistant",
            "thinking": "可选内部思考。",
            "content": "第一张..."
          }
        ],
        "images": [
          {
            "id": "img0",
            "source_type": "path",
            "path": "images/example.jpg"
          },
          {
            "id": "img1",
            "source_type": "base64",
            "data_base64": "/9j/4AAQSkZJRgABAQ...",
            "mime_type": "image/jpeg",
            "sha256": "可选但推荐"
          }
        ],
        "source": "example"
      }
      ```

      - 字段规则：单条消息的 `content` 与 `content_parts` 二选一；`content_parts` 只允许 `text` 与 `image` part；`image_id` 必须引用唯一的 `images[].id`；`images[].source_type` 只允许 `path|base64`；`source_type=path` 时必须有相对 manifest 或数据根目录的 `path`，不得有 `data_base64`；`source_type=base64` 时必须有 `data_base64` 和 `mime_type`，推荐提供输入图片文件的原始编码 bytes（base64 场景为解码后的 payload）的 `sha256`，若提供则必须比对一致；assistant 消息沿用现有 `thinking / content / tool_calls` 结构；不得把 `<image>` 写入自然文本；正式写入 README 前必须先落地 schema、batch 和 smoke 测试。
      - schema v1 固定只支持 `text|image` 和 `modality_ids={padding:0,text:1,image:2}`；未来新增模态必须升级数据 schema 并通过离线转换工具迁移，不做运行时多版本兼容。
    - [ ] 2. 图像 batch 与缓存：实现图像加载、NaFlex processor 预处理、动态 padding、同 batch 图像数量校验和 fail-closed 错误处理；异构 batch 只执行有效媒体行并按显式映射恢复，禁止零媒体/零 feature/零值伪梯度；离线批失败可逐记录定位，但必须写 rejection ledger、accepted/rejected `data_progress` 和稳定原因码，超过预登记拒绝率时失败；冻结视觉塔时支持 image feature 预计算缓存，缓存绑定 encoder、processor、NaFlex 配置、dtype、文件指纹、解码后尺寸和实际 patch token mask。
    - [ ] 3. 可选依赖隔离：`transformers / Pillow / torchvision` 仅作为多模态依赖；`multimodal_enabled=False` 时纯文本训练、推理、checkpoint 和测试不得因依赖缺失失败。
    - [ ] 4. 训练 workflow：新增 `multimodal_projector_align`、`multimodal_sft`、`multimodal_lora` recipe；推荐顺序为前向 smoke -> 纯文本 v2 checkpoint 派生 -> 冻结视觉塔训练 projector/LoRA -> 小规模多模态 SFT -> 顶层解冻 ablation。
      - manifest 按记录 `type` 分流：现有纯文本 `chat` 样本原样作为 replay 输入，多模态样本走 `multimodal_chat` batch；不复制纯文本数据，也不把 schema 版本兼容逻辑塞进运行时 loader。
    - [ ] 5. Checkpoint 与初始化：冻结视觉塔时只保存 projector、LPT/LoRA 权重和视觉塔引用/hash；解冻视觉塔时显式保存 adapter 或导出权重。新增纯文本 v2 checkpoint 派生工具，只允许白名单新增视觉权重初始化，并记录 base checkpoint、missing/unexpected key 白名单、seed、dtype/device 和 `media_attention_policy`。
    - [ ] 6. 推理入口：CLI 与 `InferenceSession` 支持图像输入和预计算 `image_features`，复用 native thinking、结构化输出、流式事件和 Function Call；媒体通过结构化 span 传递，不暴露自然语言标签或 tokenizer 哨兵；`InferenceSession` 作为生命周期/资源所有权事实源，支持 request-level 预算预检、失败回滚、事件代次/序号、单生命周期终态唯一和幂等清理，不代办 P2/P3 的共享资源池、公平调度或断线重连。
    - [ ] 7. Sequence packing 策略：MVP 可默认关闭 multimodal packing；启用时同一样本的视觉段、文本段、thinking/answer 段共享 `segment_id`，NaFlex 实际视觉 token 计入 `max_length` 和 LongRoPE2 窗口预算，且不得跨样本共享图像特征。
    - [ ] 8. Checkpoint 派生 CLI：提供离线 `tools/derive_multimodal_checkpoint.py`，执行源 `model_config_schema_version=3` 与外层 `checkpoint_schema_version=2` 严格校验、白名单复制、无媒体 logits 对比、失败报告和原子输出；目标配置升级到 `model_config_schema_version=4`，纯文本 chat replay 继续沿用现有 `chat` schema，不新增强制转换任务。
  - 评估与准入：
    - [ ] 1. 前向与训练 smoke：覆盖假视觉塔/随机 `image_features + image_token_mask` forward、真实 image processor 的 `pixel_values / pixel_attention_mask / spatial_shapes` 对应关系、小 batch projector 对齐、冻结视觉塔 LoRA 训练和 checkpoint round-trip。
    - [ ] 2. 视觉可见性准入：构造超过 `attention_window_size` 的长图文输入，分别验证 `local_window_only` 的预期边界和 `pinned_media_kv` 的可用路径。
    - [ ] 3. 数据安全准入：覆盖多图、坏图、空图、超大图、路径越界、缓存陈旧、图像数量不匹配、逐记录拒绝/恢复和可选依赖缺失；base64 输入需限制解码后大小，校验 MIME、magic bytes 与 Pillow verify，始终计算输入文件原始编码 bytes（base64 解码 payload）的 `sha256` 作为缓存指纹，样本提供 `sha256` 时必须比对一致。
    - [ ] 4. 纯文本回归准入：`multimodal_enabled=False` 时现有 `text_pretrain / chat_sft / chat_lora`、checkpoint 加载、InferenceSession、参数统计和既有单元测试保持兼容。
    - [ ] 5. 多模态质量报告：新增 caption、VQA、OCR/图表理解代理、图文检索、幻觉率、中英图像问答、长图文上下文压力测试，以及交换 `image_id -> asset`、交换 image part 顺序、删除/复制图像、future-media 前移和语义等价同轮前置/后置布局的可判定成对反事实；报告绑定 checkpoint、vision encoder、processor、NaFlex 配置、数据集版本和图像缓存指纹。
    - [ ] 6. Post-MVP 评估项：`vision_bidirectional`、pixelshuffle token reducer、connector 类型、视觉塔/projector 独立学习率、NaFlex token budget sweep、多模态 DPO/GRPO 训练组织只作为后续实验，不阻塞最小图像理解闭环。
    - [ ] 7. MM0-MM4 映射：在方案中作为第 14 项内部串行准入门；MM3 必须完成拒绝账本、禁止零值伪梯度和 Assist/decay/router 兼容组合包统一测速，发生回归时补定向诊断矩阵；MM4 必须完成请求终态/清理/逐请求隔离、多图与合法同轮布局反事实，并复用 P0 真实 checkpoint 长图文准入和统一实验目录元数据。

### P5 后续图音能力立项前置条件（非当前任务）

技术约束以 `help/LPTv2模型定型方案.md` 的“音频/语音/实时后续阶段边界”为唯一事实源，本节只浓缩未来任务拆分和立项前必须满足的条件；若摘要与定型方案不一致，必须先按定型方案修正任务摘要。这里不使用任务复选框，不新增当前任务编号、配置字段、recipe、优先级或完成进度。音频理解、语音输出或实时会话只有在另行明确立项并补齐依赖、阶段和准入后才能实现。

- **能力声明与 schema**：建立有限能力声明项清单，分别登记输入/输出组合、交互模式、原生或显式级联路径，以及 schema、数据、训练、评测、运行时和发布状态；这里的能力声明项不同于模型配置档“运行 Profile”。前向可运行或演示结果不得替代端到端准入。未来音频 schema 必须使用结构化媒体身份、消息/轮次归属、part 顺序、有效时间范围、帧 mask、时间基准和媒体到融合 span 的映射，并支持与图像在同一轮内显式绑定。
- **音频输入与安全**：立项时冻结容器/编码校验、声道与采样率规范化、重采样、有限值/削波、时长和解码资源上限、原始内容摘要、processor 指纹及原始样本到有效帧的时间映射；原生音频使用独立感知前端、temporal reducer/projector 和预算，异构 batch 禁止零音频伪样本。派生转写只能作为有 provenance 的辅助通道或显式 fallback，不得静默替换用户音频。
- **训练与能力保护**：新 connector/adapter/renderer 先隔离对齐，再以低学习率和白名单联合训练；每次新增能力声明项必须回放纯文本和所有既有已准入能力声明项。图像、输入音频、参考声学条件和历史只作条件，当前 answer 的文本、声学和终止目标绑定同一 span 并分别统计；多声学流按有效帧独立归一和报告。
- **语音渲染与复现**：LPT 只向独立 renderer 暴露版本化、已提交的 answer-channel 状态，不暴露 thinking/tool 状态。codec 私有拓扑必须满足可播放帧原子提交、终止汇聚、单调无重叠音频区间、尾块至多一次 flush 和流式/整段解码一致性；trace 绑定 bridge、renderer/codec revision、调度计划、各输出采样配置、seed/RNG 和分块连续性策略，不把固定声学流数量、码本或延迟表写入 LPT 主干。
- **实时轮次事务**：异步文本、图像和音频绑定 request generation 与 turn，经过暂存、校验和原子提交后才可消费；取消、超时和新轮次清理未提交资产，迟到资产不得串轮。内部资产消费保证恰好一次但不承诺网络事件恰好一次投递。VAD/端点基线必须登记 pre-roll、起止驻留/迟滞、尾部策略和 reset；barge-in 音频既取消旧输出又恰好一次进入下一轮，端点后整段 prefill 不得宣称流式音频编码或全双工。
- **评测与准入**：按能力声明项、语言、输入/回答时长、噪声和交互模式分桶；图像到语音分别评测视觉答案正确性与 renderer 忠实度，混合图音输入分别替换、删除或交换单一模态以测量归因。准入覆盖文本/声学/终止目标、首个可播放音频延迟、端点与打断、取消泄漏、逐流停止、流式连续性、边界伪影、自然度和韵律；自动转写只作为内容代理。
- **未来候选包**：任务立项后可评估带 provenance 的波形/特征增强与模态 dropout、受保护的 renderer self-conditioning、共享声学主体加逐流轻量 adapter、严格审计的 renderer 初始化迁移，以及只进入 renderer 的结构化说话人/风格条件。候选不得扰动媒体 span、answer-channel 或控制信息，语音克隆继续要求独立安全、授权、审计和撤销准入。

## 近期优先级

1. 用真实训练 checkpoint 跑长上下文 suite、LongRoPE2 factor sweep 和资源成本报告，决定 RetNetAssist 与 xLSTMAssist 的保留频率。
2. 补正式数据训练入口的长跑验证，确认三阶段 workflow、resume、指标和 checkpoint 在真实数据上稳定。
3. 立即落地 7.4 训练提交边界、通用 RNG、原子 checkpoint 和恢复一致性，先消除梯度累积中间发布可恢复状态的风险；分布式扩展由 P2 第 8 项复用同一语义。
4. 立即落地 7.5 纯文本批量分词与拒绝账本，冻结 accepted/rejected 集合、守恒式、原因码和拒绝率 fail-closed 门槛。
5. 并行推进 P1.5 中的 `chunked lm_head + loss`、`return_states=False` 和显存 benchmark 口径，再评估 activation checkpointing。
6. 建立 P4 核心架构与工程优化评估矩阵，沿训练阶段、运行生命周期和依赖边界形成多个成熟兼容候选包，并使用同一基准套件统一测量 Hybrid Attention、MLA / 低秩投影、MTP、残差混合和 Native thinking 上下文治理；各候选包出现回归后再定向拆分。
7. 建立 P5 多模态技术基线，优先确认 `google/siglip2-so400m-patch16-naflex` 本地加载、实际视觉特征嵌入维度与配置期望 1152 一致、patch16、可配置单图/单样本视觉预算、`image_token_mask` 变长 token 处理、视觉前缀保留策略、纯文本 checkpoint 派生、多模态可选依赖隔离和冻结视觉塔 projector 对齐 smoke；具体预算由 MM0 实施方案结合资源 smoke 和质量评估确定。pixelshuffle token reducer、connector 类型、独立学习率、NaFlex token budget sweep 和多模态 DPO/GRPO 训练组织作为 post-MVP 评估项。
8. 建立 sequence packing benchmark 的固定回归口径，定期比较 token 利用率、吞吐和 CUDA peak memory。
9. 推进第 10 点实验治理目录与元数据校验，防止后续报告不可追溯。
10. 在具备第二张 GPU 时推进第 8 点真实多卡推理 smoke 与 FSDP2 最小闭环。
11. 第 9 点服务化推理在统一执行入口稳定后推进。

## 验证记录

本清单迁移为文档变更，不修改运行时代码。当前 v2 常用验证命令如下：

```powershell
python -m compileall lpt_config lpt_model lpt_runtime lpt_data lpt_protocol lpt_eval tests tools
python -m unittest tests.test_attention_backend tests.test_model_config tests.test_lpt_v2_model tests.test_lpt_v2_state tests.test_parameter_count tests.test_runtime_execution tests.test_lpt_v2_eval_reports tests.test_lpt_v2_state_pool_checkpoint
python -m unittest tests.test_data_pipeline
python -m unittest tests.test_lpt_v2_workflows tests.test_lpt_v2_xlstm_memory
```

Windows 上 `pyarrow` 原生扩展与部分 torch 测试同进程混跑时可能异常退出；遇到时按模块拆开运行。

2026-08-01 多模态方案审计复核记录：

- 审计结论：配置冗余、token budget 拆分、P5 旧字段和 MM0-MM4 未映射属于有效问题；方案中已解决但任务清单仍残留的字段已同步修正。
- 方案补充：NaFlex reducer 约束、两区 Paged KV 生命周期、schema 升级/离线转换、派生 CLI 与失败审计、InferenceSession 字段、量化准入、长图文场景和未立项模态边界。
- P5 第 14 项新增 14.1-14.5 内部阶段映射；阶段仍属于第 14 项，不改变项目级任务范围或完成状态。
- `git diff --check -- help/LPTv2模型定型方案.md help/任务清单.md`：通过；两份文档均为 UTF-8 无 BOM/CRLF，方案 JSON 示例可解析，多模态章节无未立项阶段编号，P5 未残留主动使用的旧字段。

2026-05-28 复核记录：

- 文档变更：新增 P5 多模态核心能力专项，整理 LPT v2 原生多模态路线。
- 模型选择：P5 默认视觉塔固定为 `google/siglip2-so400m-patch16-naflex`，不规划其他视觉塔路线；原因是 NaFlex 更符合实际多分辨率、保留宽高比的图像输入。
- 关键修正：SO400M 视觉特征嵌入维度统一按 1152；NaFlex 按 patch16、可配置单图/单样本视觉预算和 `image_token_mask` 变长视觉 token 规划；不把 `<image>` 作为自然文本协议，改为原生结构化图像槽位；视觉塔默认冻结，先做 projector / LoRA 对齐 smoke。
- 工程边界修正：P5 不被单个外部多模态项目牵引；pixelshuffle token reducer、connector 类型、视觉塔/projector 独立学习率、NaFlex token budget sweep、多模态 DPO/GRPO 训练组织只作为 LPT v2 自身可评估的工程选项。
- 本次补充：P5 已按模型、工程、评估与准入三部分重组，去除“建议复核/落地清单”的重复表述；视觉前缀保留、纯文本 checkpoint 派生、可选依赖隔离、sequence packing、纯文本回归和 post-MVP 选项均已落到对应分组。
- `git diff --check -- help/任务清单.md`：通过，仅 CRLF 提示。

2026-05-27 复核记录：

- 文档变更：新增 P1.5 训练显存优化专项，覆盖 1-11 项显存优化落地清单；第 10 项只保留 Muon 优化器候选。
- 核心架构与工程优化复核：Hybrid Attention、残差混合、FP4 + FP8、MLA / 低秩投影、压缩 KV + sparse top-k、MTP、MoE routing、历史 thinking 裁剪、GRPO / KL 后训练路线均已纳入 P4；第 13 项同时保留模型能力和支撑能力落地的工程优化，并限定为可复现实验与单机优先验证范围。
- `git diff --check -- help/任务清单.md`：通过，仅 CRLF 提示。

2026-05-23 复核记录：

- `.venv\Scripts\python.exe -m compileall lpt_data lpt_training tests`：通过。
- `.venv\Scripts\python.exe -m unittest tests.test_lpt_v2_workflows`：通过，15 个测试。
- `python -m unittest tests.test_data_pipeline...`：当前 Windows 临时目录 ACL 拒绝写入，未进入被测逻辑；这是既有 `tempfile` 权限问题，本次未改测试运行器。

2026-05-24 复核记录：

- `.venv\Scripts\python.exe -m compileall lpt_protocol lpt_data lpt_config lpt_model lpt_runtime lpt_inference lpt_training lpt_workflows tools tests`：通过。
- `.venv\Scripts\python.exe .tmp_unittest\run_recheck_unittest.py tests.test_data_pipeline tests.test_lpt_v2_model tests.test_model_config`：通过，55 个测试；已覆盖 `thinking_mode=auto` 下 `thinking` 缺失/空字符串/非空三类分支。
- `.venv\Scripts\python.exe -m unittest tests.test_lpt_v2_workflows`：通过，16 个测试；已覆盖 chat SFT / chat LoRA 训练默认 `thinking_mode=auto` 与推理阶段 `chat_sft / lora / chat_lora` 默认 `on/visible`。
- `.venv\Scripts\python.exe .tmp_unittest\run_recheck_unittest.py tests.test_lpt_v2_state_pool_checkpoint tests.test_parameter_count`：通过，10 个测试。
- `.venv\Scripts\python.exe .tmp_unittest\run_recheck_unittest.py tests.test_data_pipeline tests.test_lpt_v2_workflows tests.test_lpt_v2_model tests.test_model_config`：通过，73 个测试；已覆盖 assistant `tool_calls` schema、JSON 前缀约束和 tool_call 结构化解析。
- `.venv\Scripts\python.exe main.py --help`：通过，已展示 `--stream`、`--tools-json`、`--output-format`、`--tool-choice` 参数。
- 推理 smoke：`thinking_mode=on`、`thinking_visibility=visible`、`max_thinking_tokens=2`、`max_length=2` 在 CPU tiny 模型上通过，返回结构化 `thinking` 字段。
- `data/manifests/chat_sft.json` 加载通过，当前活跃样本 1164 条；`data/structured/chat_sft` 与 `data/structured/chat_lora` 未检出 `<think>` / `</think>` 残留。
- 未通过项：`tests.test_runtime_execution` 中 `test_parse_cuda_visible_devices_distinguishes_unset_and_hidden` 仍受既有环境副作用影响，`GlobalConfig` 导入时会设置 `CUDA_VISIBLE_DEVICES=0,1`，导致未设置环境变量的断言不成立；该问题与本次 thinking 改动无关。

2026-05-04 复核记录：

- `.venv\Scripts\python.exe -m compileall lpt_config lpt_model lpt_runtime lpt_data lpt_protocol lpt_eval lpt_inference lpt_lora lpt_training lpt_workflows tools tests`：通过。
- `.venv\Scripts\python.exe .tmp_unittest\run_recheck_unittest.py tests.test_model_config tests.test_lpt_v2_model tests.test_lpt_v2_state tests.test_lpt_v2_state_pool_checkpoint tests.test_lpt_v2_eval_reports`：通过，29 个测试。`run_recheck_unittest.py` 仅修正当前沙箱下 Python `tempfile.mkdtemp()` 目录 ACL 问题，不改项目源码。
- `.venv\Scripts\python.exe .tmp_unittest\run_recheck_unittest.py tests.test_data_pipeline`：通过，12 个测试。
- `.venv\Scripts\python.exe .tmp_unittest\run_recheck_unittest.py tests.test_lpt_v2_workflows tests.test_lpt_v2_xlstm_memory`：通过，9 个测试。
- GPU 环境：torch `2.11.0+cu130`，`NVIDIA GeForce RTX 5060 Ti`，CUDA 可用。
- GPU smoke：`tools/train_lpt_v2_smoke.py --device cuda --dtype fp32 --save-checkpoint .tmp_tests\lpt_v2_recheck_1_7\gpu_smoke_model.pt` 通过，checkpoint 严格校验通过，CUDA 推理输出 `1,2,3,3,3,3,3`。
- GPU `InferenceSession` probe：prefill / append / rebuild / reset 通过，reset 后 RetNetAssist 与 xLSTM state count 均为 0。
- GPU workflow probe：`lpt_training.train()` 在 CUDA 下启用 sequence packing 与 `longrope2_window_lengths=(8,16)`，完成 1 step，保存 checkpoint、推理权重、配置与 metrics，并通过 checkpoint 严格校验。
- 第 3 点发现：v2 长上下文报告当前只支持随机初始化机制准入，不支持加载真实训练 checkpoint；任务状态已改为部分完成。

## 一句话总结

v1 原任务清单已迁移为 v2-only 版本：已完成项按 v2 模块和 schema 重新落位，未完成项聚焦 LongRoPE2 sweep、真实 checkpoint 准入、正式长跑训练、分布式执行、实验治理、服务化推理和后续架构评估。
